# Build Helpers

The scripts in this directory describe reproducible configure commands for the build-feasibility experiment.

They are intentionally downstream-only and do not modify the upstream submodule.
